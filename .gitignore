node_modules
dist
coverage
*.log
*.tgz
*.tsbuildinfo
.eslintcache

# Python
__pycache__
*.egg-info
.pytest_cache
.venv
venv

# PHP
vendor
composer.lock
.phpunit.cache
.phpunit.result.cache

# Go
*.test
*.out

# Rust
target
# Zero runtime deps — the lockfile only pins the crate itself, so it adds no value.
packages/rust/Cargo.lock

# OS + editors + secrets
.DS_Store
Thumbs.db
.env
.env.*
!.env.example
.vscode
.idea
.claude
